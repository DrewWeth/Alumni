<h1>Editing org</h1>

<%= render 'form' %>

<%= link_to 'Show', @org %> |
<%= link_to 'Back', orgs_path %>


<p>Parent Selector</p>
<select>
  <% @parents.each do |p| %>
      <option value="<%= p.id %>"><%= p.name %></option>
  <% end %>
</select>